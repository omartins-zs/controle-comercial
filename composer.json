{
    "name": "gabriel-martins/controle-comercial",
    "description": "Sistema de Controle Comercial (CodeIgniter 3 + Ion Auth) — testes de integração via HTTP contra o app rodando no Docker.",
    "type": "project",
    "license": "proprietary",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6"
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
